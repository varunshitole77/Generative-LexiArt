# Core Dependencies
streamlit==1.28.0
python-dotenv>=1.0.0
huggingface_hub>=0.19.0  # Modern InferenceClient API
requests>=2.28.0

# Image Processing (compatible versions)
Pillow>=9.0.0

# PyTorch CPU (has pre-built wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# AI/ML Libraries (if needed for local generation)
transformers>=4.30.0
iffusers>=0.21.0
accelerate>=0.20.0

# LangChain (for prompt processing) - Updated versions
langchain>=0.1.0
langchain-community>=0.0.38
langchain-core>=0.1.0

